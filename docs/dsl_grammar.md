# TIIMU Expression DSL (Text DSL) — v0.1

See: precedence, EBNF grammar, and examples.

Key operators:
- `!` NOT
- `&&` AND (short-circuit)
- `||` OR (short-circuit)
- comparisons `== != < <= > >=`
- membership `in`, `not in`
- `contains`
- regex match `~ /pattern/`
